html 
    head
        block head
            link(rel='stylesheet' href="/public/css/styles.css")
            meta(name="viewport" content="width=device-width, initial-scale=1")
            div(class="header" id="header" style="width: 100%")
                a(href="#home") Home
                a(href="#home") test
                a(href="#" style="float: right;" class="icon" id="hamburger-icon")
                    div(class="bar")
                    div(class="bar")
                    div(class="bar")
                a(href="/auth/login" style="background-color: lightgrey; float: right") Login
    body 
        block body